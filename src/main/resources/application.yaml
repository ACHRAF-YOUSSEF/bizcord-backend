spring:
  threads:
    virtual:
      enabled: true
  main:
    banner-mode: "off"
  application:
    name: Bizcord-Backend
  profiles:
    default: dev
  jpa:
    open-in-view: false

rtc:
  mediasoup:
    url: http://localhost:3000
